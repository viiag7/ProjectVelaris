# Future: Open and Click Tracking

Tracking is deferred from the initial scope.

Potential future capabilities include:

- open detection;
- click detection;
- tracking domains;
- tracking events associated with a Delivery;
- privacy and retention controls.

Open detection must not be represented as proof that a human actually read a message because image proxies and prefetching can generate events.
